@model Item
@{
    ViewData["Title"] = "Item Details";
}

<div >
    <div>
        <img src="@($"{FileSettings.ImagesPath}/{Model.Img}")" alt="@Model.ItemName" />
    </div>
    <div>
        <h1 >@Model.ItemName</h1>
        <p>@Model.ItemID</p>
        <span >@Model.Category?.Name</span>
        <p>@Model.Description</p>
        <p>@Model.Quantity</p>
        <p>@Model.Price</p>
    </div>
</div>